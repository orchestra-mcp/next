/**
 * Monaco worker configuration.
 *
 * This file is intentionally a no-op. Worker setup is handled by
 * @monaco-editor/react's default CDN loader, which works in both
 * Webpack (Next.js) and Vite without manual worker configuration.
 *
 * The previous Vite-only ?worker imports caused errors in Next.js.
 */
